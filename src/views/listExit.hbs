            <style>
               a {
                   color: inherit;
                   text-decoration: none;
                   background-color: inherit;
                   /* Otros estilos personalizados */
               }

               a:hover {
                   text-decoration: none; /* Quita el subrayado al colocar el cursor encima */
                   color: inherit; /* Mantiene el color del texto al colocar el cursor encima */
                   background-color: inherit; /* Mantiene el color de fondo al colocar el cursor encima */
                   /* Otros estilos personalizados al colocar el cursor encima */
               }
           </style>

       <div class="container p-4">
    <div class="row-4">
        {{#each projects}}
        <a href="/projectDetails/{{id}}?title={{title}}&description={{description}}&evolved={{evolved}}&threat={{threat}}&state={{state}}&creation_date={{creation_date}}">
            <div class="col-md-12 h-50">
            <div class="card text-center">
                <div class="card-body">
                        <h1 class="card-title text-uppercase">
                            {{title}}
                        </h1>
                    <p class="m-2">Involucrados: {{evolved}}</p>
                    <p class="m-2">Estado: {{state}}</p>
                    <p class="m-2">Fecha de creación: {{timeago creation_date}}</p>
                </div>
            </div>
         </div>
        </a>
         {{else}}
          <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p class="text-danger">Aún no hay proyectos guardados</p>
            </div>
          </div>
        {{/each}}
    </div>
</div>